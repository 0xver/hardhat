{
	"name": "hardhat",
	"version": "1.0.0",
	"license": "GPL-3.0-only",
	"scripts": {
		"start": "npx hardhat node",
		"compile": "npx hardhat compile",
		"test": "npx hardhat test",
		"deploy": "npx hardhat run scripts/src.deploy.js",
		"lint": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --check .",
		"format": "prettier --use-tabs --trailing-comma 'none' --plugin-search-dir . --write ."
	},
	"devDependencies": {
		"@0xver/solver": "*",
		"@nomicfoundation/hardhat-toolbox": "^2.0.1",
		"dotenv": "^16.0.3",
		"hardhat": "^2.12.7",
		"prettier": "^2.8.4",
		"prettier-plugin-solidity": "^1.1.2"
	}
}
